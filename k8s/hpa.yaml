apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: chibest-api
  namespace: chibest
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: chibest-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60


